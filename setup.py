from setuptools import setup, find_packages

setup(
    name='SnakeBuildSystem',
    version='0.1',
    author='Chris Keyser',
    description="A build system like make or cmake, but written in python",
    packages=find_packages(),
    install_requires=[],
)